const addPictureButton = document.getElementById('add-picture-button');

addPictureButton.addEventListener('click', () => {
    //set stype to display: block
    document.getElementById('image-pop-up').style.display = 'block';
}